// Smoke test for the test harness. Domain coverage lives in the engine and
// auction suites.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('test harness runs', () {
    expect(1 + 1, 2);
  });
}
